<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Data</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="title-text">Updating Data</h1>
      <form action="" method="post">
        <div class="input-section">
          Name<input
            type="text"
            name="name"
            placeholder="Insert your name"
            value="<%= item.name %>"
            required
          />
        </div>
        <div class="input-section">
          Height<input
            type="number"
            name="height"
            placeholder="Insert your height"
            value="<%= item.height %>"
          />
        </div>
        <div class="input-section">
          Weight<input
            type="number"
            step="0.01"
            name="weight"
            placeholder="Insert your weight"
            value="<%= item.weight %>"
          />
        </div>
        <div class="input-section">
          Birth Date<input
            type="date"
            class="date-add"
            name="birthdate"
            value="<%= item.birthdate %>"
          />
        </div>
        <div class="radio-edit">
          <div class="legend-radio"><legend>Is Married</legend></div>
          <div class="radio-input-edit">
            <% if( !item.married) { %>
            <input type="radio" id="false" name="married" value="0" checked />
            <label for="false">Not Yet</label><br />
            <input type="radio" id="true" name="married" value="1" />
            <label for="true">Yes</label>
            <% } else { %>
            <input type="radio" id="false" name="married" value="0" />
            <label for="false">Not Yet</label><br />
            <input type="radio" id="true" name="married" value="1" checked />
            <label for="true">Yes</label>
            <% } %>
          </div>
        </div>
        <div class="footer-add">
          <button class="btn-save-add" type="submit">Save</button>
          <button class="btn-reset"><a href="/">Cancel</a></button>
        </div>
      </form>
    </div>
  </body>
</html>
